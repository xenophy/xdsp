- hosts: localhost
  user: root
  tasks:
    - name: Install the Ubuntu packages
      apt:
        name:
          - build-essential
          - wmctrl
          - vim-gtk3
          - vim-gui-common
          - appimagelauncher
          - snapd
          - pinta
          - graphicsmagick
          - icnsutils
          - libxtst-dev
          - libx11-dev
          - libxrender-dev
          - libxkbfile-dev
          - libgconf-2-4
          - wget
